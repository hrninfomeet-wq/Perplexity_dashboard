{
  "openprj": {
    "prefix": "/openprj",
    "body": [
      "Run the complete project startup using the automated batch file: kill existing processes, start backend + frontend servers in separate windows, open Edge browser, and verify all services are running"
    ],
    "description": "Run project startup properly"
  }
}